# Copy to .env (local) or set in Vercel -> Settings -> Environment Variables. NEVER commit real keys.

# --- Form delivery (used by api/submit.js, server-side only) ---
RESEND_API_KEY=
FORMS_TO_EMAIL=afrishpetals@gmail.com
FORMS_FROM_EMAIL=Afrish Petals <bookings@afrishpetals.rw>

# --- Optional: send forms to another service instead (Formspree / Make / Zapier) ---
VITE_FORM_ENDPOINT=
VITE_CONTACT_ENDPOINT=
VITE_RESERVATION_ENDPOINT=
VITE_EVENT_ENDPOINT=
